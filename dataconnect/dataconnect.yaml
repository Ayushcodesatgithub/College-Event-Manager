specVersion: "v1"
serviceId: "eveman"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "eveman-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
